from __future__ import annotations

"""Legacy placeholder.

Diese Projektversion nutzt bewusst keinen KI-Agenten und keine OpenAI API mehr.
Die Hauptlogik liegt in app.py, xml_utils.py, pdf_renderer.py und zugferd_builder.py.
"""


def analyze_invoice_xml(*args, **kwargs):
    raise RuntimeError(
        "Der KI-Agent ist in dieser Version deaktiviert. "
        "Nutze die lokale deterministische Verarbeitung in app.py."
    )
